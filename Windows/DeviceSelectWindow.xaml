<Window x:Class="RigolWidget.Windows.DeviceSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Interface Config"
        Icon="pack://application:,,,/Assets/app.ico"
        Width="620" Height="380"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" FontFamily="Segoe UI">

    <Border Background="#1B1C20" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Border.Effect>
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="#111216" CornerRadius="6,6,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="38">
                    <TextBlock Text="Interface Config" Foreground="#F2A900"
                               FontWeight="Bold" FontSize="15" VerticalAlignment="Center" Margin="14,0,0,0"/>
                    <Button Content="✕" Click="Close_Click" Width="38" Height="38"
                            HorizontalAlignment="Right" Foreground="#C8CCD2" FontSize="14"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- USB tab (USB only) -->
            <Border Grid.Row="1" Background="#141519" Padding="14,10,14,4">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#2A2D34" CornerRadius="4,4,0,0" Padding="16,6">
                        <TextBlock Text="USB" Foreground="#EEF1F4" FontWeight="SemiBold" FontSize="13"/>
                    </Border>
                    <TextBlock Text="USB instruments only" Foreground="#7C828A" FontSize="11"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- Device list -->
            <Border Grid.Row="2" Background="#0F1013" Margin="14,0,14,0"
                    BorderBrush="#2A2D34" BorderThickness="1">
                <Grid>
                    <ListBox x:Name="DeviceList" Background="Transparent" BorderThickness="0"
                             Foreground="#DDE1E6" FontFamily="Consolas" FontSize="13"
                             MouseDoubleClick="DeviceList_MouseDoubleClick"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Foreground" Value="#DDE1E6"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2860C8"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <TextBlock x:Name="EmptyHint"
                               Text="Searching for USB devices..."
                               Foreground="#6B7077" FontSize="13"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Bottom buttons -->
            <Grid Grid.Row="3" Margin="14,12,14,14">
                <TextBlock x:Name="StatusText" Foreground="#7C828A" FontSize="11"
                           VerticalAlignment="Center" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Refresh" Click="Refresh_Click"
                            Padding="16,7" Margin="0,0,8,0" Cursor="Hand"
                            Background="#2A2D34" Foreground="#DDE1E6" BorderThickness="0" FontSize="13"/>
                    <Button x:Name="ConnectButton" Content="Connect" Click="Connect_Click"
                            Padding="22,7" Cursor="Hand" IsDefault="True"
                            Background="#F2A900" Foreground="#1A1A1A" BorderThickness="0"
                            FontWeight="SemiBold" FontSize="13"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
